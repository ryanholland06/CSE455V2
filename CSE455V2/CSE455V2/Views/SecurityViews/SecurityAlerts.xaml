<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CSE455V2.Views.SecurityViews.SecurityAlerts">
    <ContentPage.Content>
        <StackLayout Margin="20,30,20,20">
            <Label Text="Emergency Alerts"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   FontSize="Large"
                   FontAttributes="Bold" />
            <Entry x:Name="email" 
                   Placeholder="Enter Email or Enter 'ALL' for all Users" />
            <Entry x:Name="subject" 
                   Placeholder="Subject" />
            <Editor x:Name="body"
                   Placeholder="Type message" />
            <Button x:Name="sendBtn"
                    Text="Send"
                    BackgroundColor="Blue" 
                    Clicked="sendBtn_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>